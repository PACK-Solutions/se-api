ktor {
    deployment {
        port = 8000
        port = ${?PORT}
    }
    application {
        modules = [com.ps.personne.rest.AppKt.personne]
    }
}

database {
    url = "jdbc:h2:mem:personne"
    url = ${?DATABASE_URL}
    user = "h2"
    user = ${?DATABASE_USER}
    password = "h2"
    password = ${?DATABASE_PASSWORD}
    driverClassName = "org.h2.Driver"
    maximumPoolSize = 10
    minimumIdle = 5
    idleTimeout = 600000 // 10 minutes
    connectionTimeout = 30000 // 30 seconds
    maxLifetime = 1800000 // 30 minutes
}

environment {
    sandbox = false
    sandbox = ${?SANDBOX}
}
